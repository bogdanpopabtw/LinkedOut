import { TechIconsDirective } from './tech-icons.directive';

describe('TechIconsDirective', () => {
  it('should create an instance', () => {
    const directive = new TechIconsDirective();
    expect(directive).toBeTruthy();
  });
});
