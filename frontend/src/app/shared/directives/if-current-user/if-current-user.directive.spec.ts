import { IfCurrentUserDirective } from './if-current-user.directive';

describe('IfCurrentUserDirective', () => {
  it('should create an instance', () => {
    const directive = new IfCurrentUserDirective();
    expect(directive).toBeTruthy();
  });
});
